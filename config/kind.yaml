kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: tekton-cluster

nodes:
  - role: control-plane
    image: kindest/node:v1.29.2   # 👈 important
    extraPortMappings:
      - containerPort: 30097
        hostPort: 30097
        protocol: TCP
